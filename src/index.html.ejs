<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <link rel="icon" type="image/x-icon" href="/static/favicon.png">
  <title>KISS IDE Simulator</title>
  <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500&display=swap" rel="stylesheet">
  <style>
    * {
      box-sizing: border-box;
    }

    html, body {
      margin: 0;
      padding: 0;
      background-color: #dcdde1;
      
      font-family: 'Roboto', sans-serif;
      font-weight: 300;
    }

    /* Pattern styles */
    #container {
      display: grid;
      object-fit: fill;
    }

    #app {
      grid-column: 1;
      width: 100%;
      object-fit: contain;
      border-width: 5px;
      border-style: solid;
      border-color: black;
    }

    #simulator {
      grid-column: 2;
      object-fit: contain;
      border-width: 5px 5px 5px 0px;
      border-style: solid;
      border-color: black;
    }

    .app-header {
      display: flex;
      flex-wrap: wrap;
    }

    .ide-title {
      font-size: xx-large;
      text-align: center;
      flex-grow: 1;
    }

    .code {
      width: 530px;
    }

    .motors textarea, .robotStateValues textarea, .servos textarea {
      resize: none;
      text-align: center;
      font-size: large;
    }

    .motors {
      display: grid;
      grid-template: auto / auto auto auto auto auto;
    }

    .servos {
      display: grid;
      grid-template: auto / auto auto auto auto;
    }

    .motors > section, .servos > section {
      text-align: center;
    }

    .robotStateHead, .resetButton {
      display: inline-block;
      margin-left: 10px;
      margin-right: 10px;
    }

    .robotStateValues {
      display: grid;
      grid-template: auto / auto auto auto;
    }

    .robotStateValues > section {
      text-align: center;
    }

    #c {
      width: 100%;
      height: 100%;
      display: block;
    }
  </style>
</head>
<body>
  <div id="root"></div>
  <canvas id="c"></canvas>
</body>
</html>